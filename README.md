# Mabuya Camp

Standalone staff file system extracted from CashUp.

Manage staff members, create folders, upload documents/images, preview files, and drag-and-drop into folders.

## Run

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

Open http://127.0.0.1:5001
